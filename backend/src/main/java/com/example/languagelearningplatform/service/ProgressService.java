package com.example.languagelearningplatform.service;

import org.springframework.stereotype.Service;
import com.example.languagelearningplatform.model.User;

@Service
public class ProgressService {

    public String getProgress(User user) {
        // Placeholder logic to get user's progress
        return "User's progress information";
    }
}
